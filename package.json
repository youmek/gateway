{
  "name": "gateway",
  "version": "1.0.1",
  "description": "Gateway acting as an entry point for client applications, routing requests to the appropriate backend microservices.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Youssef Mekki",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "express-http-proxy": "^2.1.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express-http-proxy": "^1.6.6",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
